<?xml version="1.0" encoding="utf-8"?>
<!-- ============================================================================
  AGENT GUIDE — Grid báo cáo tạo mẫu.
  * Cột khớp recordset của {{proc}}; cặp name_decimal_1/2 vs _3/4 là mẫu
    ẩn/hiện theo mau_bc (xem _hiddenFields trong filter.xml).
  * JS _nam/%kn/%kt: header cột có thể chứa %kn (năm nay)/%kt (năm trước) —
    proc trả header, JS thay bằng năm thật.
============================================================================ -->
<!DOCTYPE grid [
  <!ENTITY XMLWhenReportLoading SYSTEM "..\Include\XML\WhenReportLoading.xml">
  <!ENTITY XMLWhenReportClosing SYSTEM "..\Include\XML\WhenReportClosing.xml">
  <!ENTITY XMLStandardReportToolbar SYSTEM "..\Include\XML\StandardReportToolbar.xml">
  <!ENTITY JavascriptReportInit SYSTEM "..\Include\Javascript\ReportInit.txt">

  <!ENTITY % Control.Filter SYSTEM "..\Include\Filter.ent">
  %Control.Filter;
]>

<grid type="Report" xmlns="urn:schemas-fast-com:data-grid">
  <title v="{{title_v}}" e="{{title_e}}"></title>
  <subTitle v="%f — từ ngày %d1 đến ngày %d2" e="%f — from %d1 to %d2"></subTitle>
  <fields> 
    <field name="name_string_1" width="300" allowSorting="true" allowFilter="true">
      <header v="Chỉ tiêu" e="Item"></header>
    </field> 
    <field name="name_decimal_1" type="Decimal" dataFormatString="@baseCurrencyAmountViewFormat" width="120" allowSorting="true" allowFilter="&GridReportAllowFilter.Number;" aggregate="Sum">
      <header v="Số tiền 1" e="Amount 1"></header>
    </field>
    <field name="name_decimal_2" type="Decimal" dataFormatString="@baseCurrencyAmountViewFormat" width="120" allowSorting="true" allowFilter="&GridReportAllowFilter.Number;" aggregate="Sum">
      <header v="Số tiền 2" e="Amount 2"></header>
    </field>
    <field name="name_decimal_3" type="Decimal" dataFormatString="@foreignCurrencyAmountViewFormat" width="120" allowSorting="true" allowFilter="&GridReportAllowFilter.Number;" aggregate="Sum">
      <header v="Số tiền 1 nt" e="Amount 1 FC"></header>
    </field>
    <field name="name_decimal_4" type="Decimal" dataFormatString="@foreignCurrencyAmountViewFormat" width="120" allowSorting="true" allowFilter="&GridReportAllowFilter.Number;" aggregate="Sum">
      <header v="Số tiền 2 nt" e="Amount 2 FC"></header>
    </field>
  </fields>

  <views>
    <view id="Grid">
      <field name="name_string_1"/>
      <field name="name_decimal_1"/>
      <field name="name_decimal_2"/>
      <field name="name_decimal_3"/>
      <field name="name_decimal_4"/>
    </view>
  </views>

  <commands>
    &XMLWhenReportLoading;
    &XMLWhenReportClosing;
  </commands>

  <script>
    <text>
   <![CDATA[
function load$GridReport$(g) {g.add_onRender(on$GridReport$Render);}
function dispose$GridReport$(g) {try {g.remove_onRender(on$GridReport$Render);} catch (ex) {}}
function on$GridReport$Render(sender, eventArgs) {
  var g = eventArgs.grid;  
  if (g._hiddenFields) {
    var a = g._hiddenFields, h = '';
    for (var i = 0; i < a.length; i++) {
      setGridHiddenFields(g, a[i].Fields, a[i].Value);
      if (a[i].Value) h += (h == '' ? '' : ',') + a[i].Fields;
    }
    g._externalHiddenFields = h;
  }
  var nam_truoc = g._nam - 1, nam_nay = g._nam, id = g.get_id();
  g._allFields.forEach(
      function(item) {
          var header = id + '_HeaderText_' + item.Name;
          var HeaderText = item.HeaderText.replace('%kn', nam_nay).replace('%kt', nam_truoc);
          document.getElementById(header).innerHTML = HeaderText;
      }
  )
}
function setGridHiddenFields(g, c, v) {
  var a = c.split(',');
  for (var i = 0; i < a.length; i++) {
    var l = g._getColumnOrder($func.trim(a[i]));
    if (l != -1) g._setColumnVisible(l, !v);
  }
}
function beforeDrillDown(g) {g._args.cancelEvent = !($func.trim(g._getItemValue(g._activeRow, g._activeColumn)));}
function formatDateView(c) {return String.format('{0}/{1}/{2}', c.substring(6, 8), c.substring(4, 6), c.substring(0, 4));}
]]>
    </text>
  </script>

  &XMLStandardReportToolbar;
</grid>

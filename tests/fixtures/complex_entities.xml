<?xml version="1.0" encoding="utf-8"?>
<!-- FastBusiness XML with multiple ENTITY declarations -->
<!DOCTYPE dir [
  <!ENTITY XMLWhenDirLoading SYSTEM "../Include/XML/WhenDirLoading.xml">
  <!ENTITY XMLWhenDirClosing SYSTEM "../Include/XML/WhenDirClosing.xml">
  <!ENTITY CommandCheckLockedDate SYSTEM "../Include/Command/CheckLockedDate.txt">
  <!ENTITY ScriptDirInit SYSTEM "../Include/Javascript/DirInit.txt">
  <!ENTITY ScriptGridFunction SYSTEM "../Include/Javascript/GridFunction.txt">
  <!ENTITY XMLUserDefinedFields SYSTEM "../Include/XML/UserDefinedFields.txt">
  <!ENTITY b SYSTEM "./Include/BaseCurrency.xml">
  <!ENTITY f SYSTEM "./Include/ForeignCurrency.xml">
  <!ENTITY p "../images/pdf.gif">
  <!ENTITY e "../images/excel.gif">
]>
<dir table="m91$000000" code="stt_rec" type="Voucher">
  <title v="Phiếu nhập kho" e="Purchase Receipt"/>

  <fields>
    <field name="stt_rec" isPrimaryKey="true" hidden="true">
      <header v="" e=""/>
    </field>

    <field name="ngay_ct" type="DateTime" allowNulls="false">
      <header v="Ngày chứng từ" e="Date"/>
    </field>

    <field name="so_ct" allowNulls="false">
      <header v="Số chứng từ" e="Number"/>
    </field>

    <!-- Lookup field with companion -->
    <field name="ma_kh">
      <header v="Mã khách hàng" e="Customer Code"/>
      <items style="AutoComplete"
             controller="Customer"
             reference="ten_kh%l"
             key="status = '1'"
             information="ma_kh$dmkh.ten_kh%l"/>
    </field>

    <field name="ten_kh%l" external="true" readOnly="true">
      <header v="" e=""/>
    </field>

    <!-- Grid detail reference -->
    <field name="d91" external="true" rows="144">
      <header v="" e=""/>
      <items style="Grid" controller="PRDetail" row="1">
        <item value="ForeignKey">
          <text v="String: stt_rec, stt_rec"/>
        </item>
      </items>
    </field>
  </fields>

  <commands>
    &XMLWhenDirLoading;

    <command event="Inserting">
      <text><![CDATA[
-- Validation
if exists(select 1 from @@master where so_ct = @so_ct and ngay_ct = @ngay_ct) begin
  select 'so_ct' as field, N'Số chứng từ đã tồn tại' as message
  return
end

-- Get identity
select @stt_rec = dbo.FastBusiness$GetIdentity(@@id)

-- Insert master
insert into @@master (stt_rec, ngay_ct, so_ct, ma_kh, status)
values (@stt_rec, @ngay_ct, @so_ct, @ma_kh, '1')

-- Insert detail with partition
insert into @@prime$partition$current
select * from @d91
      ]]></text>
    </command>

    <command event="Updating">
      <text><![CDATA[
-- Check if date changed (cross-month update)
#IF $ngay_ct.NewValue <> $ngay_ct.OldValue #THEN
  delete @@prime$partition$previous where stt_rec = @stt_rec
#END

-- Update master
update @@master
set ngay_ct = @ngay_ct,
    so_ct = @so_ct,
    ma_kh = @ma_kh,
    datetime2 = getdate()
where stt_rec = @stt_rec

-- Update detail
delete @@prime$partition$current where stt_rec = @stt_rec
insert into @@prime$partition$current
select * from @d91
      ]]></text>
    </command>

    &CommandCheckLockedDate;
    &XMLWhenDirClosing;
  </commands>

  <script>
    <text>
      &ScriptDirInit;
      <![CDATA[
function init$Form$(f) {
  f.add_onResponseComplete(on$Form$ResponseComplete);
}

function active$Form$(f) {
  // Set default values for new record
  if (f._action == 'New') {
    f.setItemValue('ngay_ct', new Date());
  }
}

function on$Form$ResponseComplete(sender, e) {
  var f = e.object;
  var context = e.type.Context;
  var result = e.type.Result;

  switch(context) {
    case 'GetCustomerInfo':
      if (result.length > 0) {
        var maKH = result[0].Value;   // ma_kh (column 1)
        var tenKH = result[1].Value;  // ten_kh (column 2)
        var diaChi = result[2].Value; // dia_chi (column 3)

        f.setItemValue('ten_kh%l', tenKH);
      }
      break;
  }
}
      ]]>
    </text>
  </script>

  <response>
    <action id="GetCustomerInfo">
      <text><![CDATA[
select ma_kh, ten_kh, dia_chi, dien_thoai
from dmkh
where ma_kh = @ma_kh
return
      ]]></text>
    </action>
  </response>
</dir>

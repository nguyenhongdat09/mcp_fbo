# Config local (machine paths)

Tách **path theo máy** khỏi `config.yaml` dùng chung khi ship MCP / pull repo.

| File | Vai trò |
|------|---------|
| [01_split_machine_paths.md](./01_split_machine_paths.md) | BA + QA + prompt Gemini: `config_path.yaml` (FIX từ nhầm `.xml`) |

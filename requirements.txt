# FastBusiness MCP Server - Production Dependencies
# Install: pip install -r requirements.txt

# Core MCP framework
mcp>=0.9.0

# XML parsing with entity handling
lxml>=5.0.0

# Data validation and serialization
pydantic>=2.0.0

# Async SQLite support
aiosqlite>=0.19.0

# LevelDB database support
plyvel>=1.5.0

# YAML configuration support (if needed)
pyyaml>=6.0.0
